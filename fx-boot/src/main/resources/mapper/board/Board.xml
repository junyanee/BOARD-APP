<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.board.mapper.BoardMapper">

<select id="getBoard" resultType="com.board.board.model.BoardMaster">
{
	call SP_GetBoard
	(

	)
}
</select>

<insert id="insertArticle" parameterType="com.board.board.model.BoardMaster">
{
	call SP_InsertBoard
	(
		@Title = #{title, mode = IN, jdbcType = VARCHAR},
		@Contents = #{contents, mode = IN, jdbcType = VARCHAR},
		@InsUser = #{insuser, mode = IN, jdbcType = VARCHAR},
		@ModUser = #{moduser, mode = IN, jdbcType = VARCHAR}
	)
}
</insert>

<select id="getArticle" parameterType="int" resultType="com.board.board.model.BoardMaster">
{
	call SP_GetArticle
	(
		@Idx = #{idx, mode = IN, jdbcType = INTEGER}
	)
}
</select>

<update id = "updateReadCnt" parameterType = "int">
{
	call SP_UpdateReadCnt
	(
		@Idx = #{idx, mode = IN, jdbcType = INTEGER}
	)
}
</update>

<update id = "modifyArticle" parameterType = "com.board.board.model.BoardMaster">
{
	call SP_UpdateArticle
	(
		@Idx = #{idx, mode = IN, jdbcType = INTEGER},
		@Title = #{title, mode = IN, jdbcType = VARCHAR},
		@Contents = #{contents, mode = IN, jdbcType = VARCHAR},
		@ModUser = #{moduser, mode = IN, jdbcType = VARCHAR}
	)
}
</update>

<update id = "deleteArticle" parameterType = "com.board.board.model.BoardMaster">
{
	call SP_DeleteArticle
	(
		@Idx = #{idx, mode = IN, jdbcType = INTEGER},
		@ModUser = #{moduser, mode = IN, jdbcType = VARCHAR}
	)
}
</update>

</mapper>